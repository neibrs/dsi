langcode: en
status: true
dependencies:
  enforced:
    module:
      - dsi_device_other_subtype
id: em
label: 'Environment Monitor'
locations:
  server_room: server_room
  storage_room: storage_room
  rack: rack
  blade_chassis: '0'
  building: '0'
